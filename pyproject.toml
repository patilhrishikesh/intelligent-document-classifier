[project]
name = "document-classifier"
version = "0.1.0"
description = "Intelligent Document Classifier"
requires-python = ">=3.9"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
